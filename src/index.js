/**
 * Install the plugin.
 *
 * @param {Vue} app - The Vue app to install into.
 */
const install = (app) => {
  console.log('vue-is-loading is installed!');
};

export {
  install,
  loadable,
  mapLoadableMethods,
};